import { Meta } from '@storybook/addon-docs/blocks';
import { ZStack } from './_pages/Dimension';

<Meta title="Foundations/Z-index" />

# Z-index

Seven layers, ranked. `--z-toast` sits above everything including a sheet, on
the stated grounds that a "session saved" confirmation must be visible over an
open sheet.

<ZStack />
